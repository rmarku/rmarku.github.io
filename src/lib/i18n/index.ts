import { SupportedLanguages, defaultNS, fallbackLng, fallbackNS, languages } from './settings'
import { getOptions, initI18next, useTranslation } from './utils'

export { defaultNS, fallbackNS, fallbackLng, languages, useTranslation, getOptions, initI18next }
export type { SupportedLanguages }
